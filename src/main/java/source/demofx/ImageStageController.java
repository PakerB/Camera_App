package source.demofx;

import javafx.event.ActionEvent;
import javafx.fxml.FXML;

public class ImageStageController extends Controller{
    @Override
    public void back(ActionEvent event) {
        super.back(event);
    }
}
